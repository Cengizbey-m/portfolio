import { LibraryShell } from "@/components/library/LibraryShell";

export const metadata = {
  title: "Library",
  description: "Cengiz's project library + arcade. Browse games (projects), see hours played, and launch the mini-games.",
};

export default function LibraryPage() {
  return <LibraryShell />;
}
